node_modules
README.md

# Build output: main.js and styles.css are generated by esbuild, .test-build by
# the test script. package.json and package-lock.json are left to npm, which
# rewrites their indentation whenever a dependency changes.
main.js
styles.css
.test-build
package.json
package-lock.json

data.json
