# TODO

Open work, kept alongside the notes in Claude's memory directory for this repo
(`memory/MEMORY.md` there is the index). When picking one of these up, read the
matching memory first - it carries the reasoning and the options already weighed.
Delete an entry from both places once it ships.

Nothing open.
